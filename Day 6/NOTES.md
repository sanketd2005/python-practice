Recursion : a way of solving a problem by having a function calling itself

when you will apply Recursion :
    - when a main problem can be able to divide similar sub problems then we can use the RECURSION